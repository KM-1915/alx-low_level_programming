#include"main.h"
#include<stdio.h>
#include<stdlib.h>
/**
 * **strtow - splits a string into words
 * @str: string
 * Return: a pointer to an array of strings (words)
 * NULL if str == NULL or str == "", or fails
 */
char **strtow(char *str)
{

}
